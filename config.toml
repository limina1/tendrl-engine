[embedding]
backend = "python"
dimensions = 384
enabled = true
model = "all-MiniLM-L6-v2"
sidecar_url = "http://localhost:3031"

[identity]
assistant = "npub1upulrz5tvvphu37c5ygs8xc6xpg30yds799dzspey7rsvrpgyzdqa8r477"
pubkey = "npub1m3xdppkd0njmrqe2ma8a6ys39zvgp5k8u22mev8xsnqp4nh80srqhqa5sf"

[network]
mode = "auto"

[relay]
timeout_ms = 15000
# Bootstrap seed: used ONLY on first boot (when <data_dir>/relays.json
# doesn't exist) to populate the engine's working relay sets. After
# that, the local state file is authoritative — edit relays via the UI
# (the modeline `relays` pill) and changes persist there.
initial_relays = [
    "ws://localhost:3334",
    "wss://spatia-arcana.com",
    "wss://theforest.nostr1.com",
    "wss://relay.damus.io",
    "wss://nos.lol",
]
