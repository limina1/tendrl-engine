#+TITLE: Fix: Search Response Latency
#+TYPE: fix
#+STATUS: in-progress
#+DATE: 2026-08-12
#+PROGRESS: 2026-08-14 — backfill detached + existence batched + index
#+PROGRESS: fallback; concurrent =|= branches still open

* The issue (observed 2026-08-12, v0.8.2)

Every search *awaits* =backfill_result_profiles= before responding
(=api.rs:611=, =:646=; definition =:662-684=): one =profile_exists= DB
query per result author, then a *sequential relay fanout across the
union of all relay sets* (=engine.rs:2285=, =:2305=). Local results are
in hand and then wait on the network. Compound =|= queries also run
their branches sequentially (=api.rs:566-595=), each branch potentially
an exhaustive scan plus its own relay fanout.

* Why it matters

Search is the highest-frequency interactive surface; a relay round trip
(with [[file:fix-relay-connection-churn.org][fresh-socket cost]] and a
15 s ceiling per relay) is added to every query even when everything
needed is local. The web already has a late-hydration path for profiles
(=prefetchProfiles=, debounced), so the await buys nothing the UI needs.

* Proposed fix

- [X] Return local results immediately; run the profile backfill
  detached (results carry pubkeys; the existing web profile hydration
  fills names as they land) — 2026-08-14, =backfill_result_profiles=
  spawns instead of awaiting. Measured with a blackhole relay in the
  fan-out: the search answered in 13 ms while the backfill went on
  reaching for 55 profiles across 4 relays in the background
- [X] Batch profile-existence checks into one multi-author filter
  (see [[file:fix-ndb-lock-hold.org][lock hold]]) — 2026-08-14,
  =query::profiles_missing=; also replaces the per-author
  =has_cached_profile= loop in =backfill_publication_sections=, where a
  deep tree took the process-wide lock once per author
- [X] Index = the profile-metadata fallback, per
  [[file:idea-relay-kind-routing.org][kind-scoped routing]] — 2026-08-14:
  the backfill runs its primary pass, re-checks what's still missing, and
  only then drops to =indexer.default=, then =indexer.fallback=. A hit in
  the primary set means the indexers are never contacted;
  =exclusive.indexer= still makes the indexers replace the primary set
- [ ] Run compound =|= branches concurrently (bounded) — still open

* The structural reading (added 2026-08-14)

[[file:idea-relay-kind-routing.org][Kind-scoped relay routing]] reaches
the same fix from the other side: a profile lookup is a *different*
fetch to *different* relays than the content request that triggered it
— the relay that served an event is not the relay that carries its
author's kind 0. So the await isn't merely slow, it's a category error;
and the =index= role (profile metadata to drop to) is the route that
should serve it.

* Related

- [[file:fix-index.org][Fix index]] · [[file:index.org][Index]]
- [[file:idea-relay-kind-routing.org][Kind-scoped relay routing]]
- [[file:eval-performance-2026-08.org][Performance evaluation (2026-08-12)]]
- [[file:feat-search.org][Search]] · [[file:feat-profiles.org][Profiles]]
- [[file:fix-relay-connection-churn.org][Relay connection churn]]
